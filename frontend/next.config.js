/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* We can specify environment configuration or other overrides here if needed */
};

module.exports = nextConfig;
